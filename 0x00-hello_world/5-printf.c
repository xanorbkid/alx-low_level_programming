#include <stdio.h>
/**
 * main - A simple program that print line using the printf function
 * Return: 0 (Success)
 */
int(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
